// Entry point is now src/router.ts — RouterProvider renders the route tree.
// This file is kept as a no-op so existing imports don't break during transition.
export default function App() {
  return null;
}
